HTTP_ADDR=:8080
MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE=fark_noi
WORKSPACE_ID=wsp_demo
JWT_SECRET=local-development-secret-change-me
DEV_COLLABORATOR_ID=col_demo
CORS_ORIGINS=http://localhost:3000
MCP_ENABLED=false
MCP_PATH=/mcp
# Browser JWTs authenticate the API-key management endpoints. CLI uses opaque
# lws_prj_* API keys, stored server-side only as SHA-256 hashes.
